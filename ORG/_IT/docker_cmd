# show all containters and get id

sudo docker containter ls

# get into docker

sudo docker exec -t -i d3a /bin/bash

# copy from linux > docker

docker cp container_id:/foo.txt foo.txt

sudo docker cp . d3a:/home/orto/orto_siedlce

